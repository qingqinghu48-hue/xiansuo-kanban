# 2026-04-14 工作记录

## 字段映射修复（关键）

`dashboard_data.json` 实际字段名与 `generate_html.py` 中的 JS 代码不一致，导致表格全部显示空白：

| 错误写法 | 正确字段 |
|---------|---------|
| `客户姓名` | `姓名` |
| `客户电话` | `手机号` |
| `有效性` | `线索有效性` |
| `v(r,'所属大区','大区')` | `r['所属大区']` |
| `v(r,'所属招商','招商员')` | `r['所属招商']` |

已全部修复，包括筛选逻辑、KPI 统计、表格渲染、详情弹窗。

## 成本数据来源

抖音成本数据**只从** `抖音营销线索成本统计.xlsx` 读取，不再硬编码。字段：`日期`(YYYY.MM.DD) → 当日营销总消耗 / 当日单条线索成本。成本面板独立成一行，全宽显示。

## 面板调整

- 移除「每日入库趋势」面板（`cvTrend` / `drawTrend`）
- 平台来源分布饼图新增扇区内数据标签（数量 + 百分比）
- 有效性筛选下拉新增所有实际枚举值（普通线索、无意向客户、无效线索）
