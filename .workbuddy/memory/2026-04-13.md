# 2026-04-13 工作日志

## generate_html.py 彻底重写

**问题根因**：之前多次零散修改把 `generate_html.py` 改乱，导致 HTML 模板里的 `%%ECHARTS_JS%%` 等占位符未被正确替换，ECharts 未加载，页面永远卡在"数据加载中"。

**修复方案**：完整重写 `generate_html.py`，使用 `r'''...'''` 原始字符串防止转义问题，三处占位符 `%%DATA_JS%%`、`%%COST_JS%%`、`%%ECHARTS_JS%%` 各自独立替换，验证全部通过（7项 OK）。

**当前状态**：
- `线索看板.html` 1.4MB，ECharts 内嵌 + 数据内嵌，零外部依赖
- 数据 594 条（抖音 409 / 小红书 132 / 其他 53）
- 4月12日抖音客资独有5条已收录（特殊批次）

## 2026-04-14 布局修复

发现 CSS 语法错误：`.pages button:hover{background:var(--primary-soft}` 缺少 `)`，导致后续所有样式失效、面板错乱。已修复，同步更新 `generate_html.py`。
- 仅 2026-04-12 批次的抖音客资独有线索直接收录（其他批次丢弃）
- 该规则已写入 MEMORY.md

## Skill 备份
- `leads-dashboard.zip` 已保存到桌面（含 SKILL.md + 两个脚本）
