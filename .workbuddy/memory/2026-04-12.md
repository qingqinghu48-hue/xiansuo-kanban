# 2026-04-12 工作日志

## 线索看板生成

- 读取了抖音客资（4个xlsx文件）、小红书客资（2个csv文件）、招商线索管理表（线索明细sheet，6600条）
- 关键字段：抖音用`线索创建时间`、小红书用`线索生成时间`作为入库时间；通过手机号与招商线索管理表匹配
- 线索有效性枚举值：意向客户、一般客户、未联系上、无效线索、无意向客户
- 生成了 `c:\Users\Administrator\Desktop\线索\线索看板.html`（含6条统计卡片、5个ECharts图表、分页明细表格、下钻弹窗、多维筛选）
- **数据口径**：仅收录 2026-03-01 起入库的线索
- **入库时间来源**（v2版，修正）：
  - 抖音线索 → 抖音客资 xlsx 的「线索创建时间」
  - 小红书线索 → 小红书客资 csv 的「线索生成时间」
  - 其他来源 → 招商线索管理表
  - 手机号全局去重（避免管理表与客资表重复）
- **重要发现**：抖音客资 xlsx 有19列丰富数据（之前用 read_only=True 误读为空）
- 看板数据：589条线索，抖音404条、小红书132条、其他53条，日期范围2026-03-01~2026-04-12，抖音总消耗8.9万，单条成本220.77元
- **成本数据**：来自「抖音营销线索成本统计.xlsx」（日期/营销总消耗/线索总成本/营销线索成本），每条抖音线索附加当日抖音消耗字段
- 看板图表：每日趋势 / 平台分布 / 有效性分布 / **线索成本分析（柱状消耗+折线单条成本）** / 8个KPI卡片（含抖音总消耗/单条成本）
- **技术方案（v2）**：ECharts 库从 `echarts.min.js` 下载后直接内嵌进 HTML（1.2MB单文件）；数据 `const ALL = [...]` 内嵌；零外部依赖，双击直接打开
- **重要注意**：系统 `python.exe` 快捷方式失效，所有脚本必须用 `C:\Users\Administrator\.workbuddy\binaries\python\versions\3.13.12\python.exe` 绝对路径运行
- **generate_html.py** 现在会自动下载 + 内嵌 ECharts，以后只需跑这一个脚本即可生成完整单文件
- 辅助脚本：build_dashboard.py（数据口径）、generate_html.py（HTML生成）、dashboard_data.json（中间JSON）

